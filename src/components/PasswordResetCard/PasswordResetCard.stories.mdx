import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';
import PasswordResetCard from './PasswordResetCard';
import { useState } from 'react';

export const Template = (props) => {
  const [username, setUsername] = useState('');
  const message = '';
  const handleClick = () => alert(username);
  return (<PasswordResetCard handleForgotPassSubmit={handleClick} error={''} loading={false} setUsername={setUsername} message={message} />);
};

<Meta
  title='Components/PasswordResetCard'
  component={PasswordResetCard}
/>

# Taskbar
<Description of={PasswordResetCard}/>


## Example
<Canvas>
  <Story name='PasswordResetCard'>
    {Template.bind({})}
  </Story>
</Canvas>

# Props
<ArgsTable story={'PasswordResetCard'} />