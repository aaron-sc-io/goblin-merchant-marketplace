import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';
import ProfilePage from './ProfilePage';
import { useState } from 'react';
import listingFixture from './ListingFixture.js'

export const Template = (props) => {
  const username = 'username'
  const repScore = 123
  const discord = 'discord'
  return (
    <ProfilePage
      username={username}
      repScore={repScore}
      discord={discord}
      listingData={listingFixture}
    />);
};

<Meta
  title='Components/ProfilePage'
  component={ProfilePage}
/>

# Taskbar
<Description of={ProfilePage}/>


## Example
<Canvas>
  <Story name='ProfilePage'>
    {Template.bind({})}
  </Story>
</Canvas>

# Props
<ArgsTable story={'ProfilePage'} />