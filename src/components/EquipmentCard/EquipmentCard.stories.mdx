import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';
import { useFormik } from 'formik';
import EquipmentCard from './EquipmentCard';
import { Grid, TextField, Box, MenuItem } from '@mui/material';
import { itemSchemas } from '../../utility/itemSchemas';

export const Template = (props) => {
  const handleClick = () => alert('Directory');
  const armingSword = itemSchemas[0];
  const listingData = {
    listingPrice: '300',
    finalSellingPrice: '',
    listingId: '',
    contactInfo: '--------------#0001'
  };
  const armingSwordListing = {...armingSword, ...listingData };
  const formik = useFormik({
    initialValues: armingSwordListing
  });
  return (
    <EquipmentCard 
      data={formik.values}
      isDisabled={false} 
      handleClick={handleClick} />
  )
};

<Meta
  title='Components/EquipmentCard'
  component={EquipmentCard}
/>

# EquipmentCard
<Description of={EquipmentCard}/>


## Example
<Canvas>
  <Story name='EquipmentCard'>
    {Template.bind({})}
  </Story>
</Canvas>

# Props
<ArgsTable story={'EquipmentCard'} />

