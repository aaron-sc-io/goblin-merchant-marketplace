import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';
import Taskbar from './Taskbar'

export const Template = (props) => {
  const handleClick = () => alert('Directory');
  return (<Taskbar handleClick={handleClick} />);
};

<Meta
  title='Components/Taskbar'
  component={Taskbar}
/>

# Taskbar
<Description of={Taskbar}/>


## Example
<Canvas>
  <Story name='Taskbar'>
    {Template.bind({})}
  </Story>
</Canvas>

# Props
<ArgsTable story={'Taskbar'} />