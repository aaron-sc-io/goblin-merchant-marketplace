import { TextField, Box, Button } from '@mui/material';

const Taskbar = ({}) => {
  return(
    <Box sx={{ display: 'flex', flexDirection: 'row', height: '75px' }}>
      
    </Box>
  );
};


// toolbar on top with buttons and such with height of 75 maybe



export default Taskbar;