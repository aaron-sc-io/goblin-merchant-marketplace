import { AuthProvider } from "./contexts/AuthContext";
import LoginDemo from "./LoginDemo";

const App = () => {
  return (
    <AuthProvider>
      {/* <LoginDemo /> */}
    </AuthProvider>
  )
};

export default App;
